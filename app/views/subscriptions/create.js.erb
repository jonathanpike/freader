// Clear text area in form
$("#new_subscription_form").find('input[type=url],textarea').val('')

// If form was visible, set to invisible
if (visible == true) {
  visible = false; 
}

$(".sidebar").html("<%= j render partial: 'sidebar' %>");

<% flash.each do |type, message| %>
  $("#flash-messages").html("<%= j content_tag :p, message.html_safe, class: "flash text-center" %>");
<% end %>